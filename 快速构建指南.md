# 🚀 快速构建指南

## 📦 什么是Docker镜像？

**简单理解：**
- 🏠 **镜像** = 软件安装包（包含代码+环境+依赖）
- 🏡 **容器** = 运行起来的软件实例
- 📦 **构建镜像** = 把代码打包成安装包

**为什么需要自己构建？**
- ✅ 修改代码后需要重新打包
- ✅ 添加新的依赖需要重新打包
- ✅ 可以完全控制构建过程
- ✅ 不依赖别人的镜像仓库

---

## 🛠️ 快速开始（3步搞定）

### 步骤1：构建镜像

**Windows系统：**
```bash
# 双击运行或在命令行执行
build-local.bat
```

**Linux/Mac系统：**
```bash
chmod +x build-local.sh
./build-local.sh
```

**手动构建（如果脚本不工作）：**
```bash
# 1. 构建基础镜像（10-30分钟）
docker build -t xiaozhi-esp32-server:server-base -f ./Dockerfile-server-base .

# 2. 构建应用镜像（1-3分钟）
docker build -t xiaozhi-esp32-server:server_latest -f ./Dockerfile-server-local .

# 3. 构建Web镜像（5-15分钟）
docker build -t xiaozhi-esp32-server:web_latest -f ./Dockerfile-web .
```

### 步骤2：修改docker-compose配置

编辑 `main/xiaozhi-server/docker-compose_all.yml`：

**修改第7行：**
```yaml
# 修改前：
image: ghcr.nju.edu.cn/xinnan-tech/xiaozhi-esp32-server:server_latest

# 修改后：
image: xiaozhi-esp32-server:server_latest
```

**修改第32行：**
```yaml
# 修改前：
image: ghcr.nju.edu.cn/xinnan-tech/xiaozhi-esp32-server:web_latest

# 修改后：
image: xiaozhi-esp32-server:web_latest
```

### 步骤3：启动服务

```bash
cd main/xiaozhi-server
docker-compose -f docker-compose_all.yml up -d
```

---

## ✅ 验证构建成功

```bash
# 查看镜像列表
docker images | grep xiaozhi-esp32-server

# 应该看到3个镜像：
# xiaozhi-esp32-server   server_latest
# xiaozhi-esp32-server   server-base
# xiaozhi-esp32-server   web_latest
```

---

## 🔄 修改代码后重新构建

**只修改了Python代码（plugins_func、core等）：**
```bash
# 只需重新构建server镜像（快）
docker build -t xiaozhi-esp32-server:server_latest -f ./Dockerfile-server-local .
cd main/xiaozhi-server
docker-compose -f docker-compose_all.yml restart xiaozhi-esp32-server
```

**修改了requirements.txt（添加Python包）：**
```bash
# 需要重新构建base和server（慢）
docker build -t xiaozhi-esp32-server:server-base -f ./Dockerfile-server-base .
docker build -t xiaozhi-esp32-server:server_latest -f ./Dockerfile-server-local .
```

**修改了前端或后端代码：**
```bash
# 重新构建web镜像
docker build -t xiaozhi-esp32-server:web_latest -f ./Dockerfile-web .
cd main/xiaozhi-server
docker-compose -f docker-compose_all.yml restart xiaozhi-esp32-server-web
```

---

## 📚 详细文档

更多详细信息请查看：`docs/本地构建镜像完整指南.md`

---

## ❓ 常见问题

**Q: 构建很慢怎么办？**
A: 正常，base镜像需要下载很多Python包，首次构建需要10-30分钟。

**Q: 构建失败怎么办？**
A: 检查网络连接，确保能访问Docker Hub和pip镜像源。

**Q: 镜像太大怎么办？**
A: 正常，server镜像包含AI模型依赖，通常2-3GB。

**Q: 修改代码后必须重新构建吗？**
A: 开发时可以用Volume挂载，生产环境需要重新构建。












