# 手动更新数据库指南

## 更新时间
2026-01-26

---

## 一、更新镜像（不影响数据库）

### 1.1 拉取最新镜像

```bash
# 拉取最新的server镜像
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server

# 或使用国内加速
docker pull ghcr.nju.edu.cn/alice0422/xiaozhi-esp32-server:server
```

### 1.2 重启服务

```bash
# 停止当前服务
docker-compose down

# 启动新版本
docker-compose up -d
```

**注意：** 这样更新只会更新代码，不会修改数据库。

---

## 二、手动更新数据库（可选）

如果需要使用新增的功能（CMHK ASR/TTS、阿里百炼ASR等），需要手动执行SQL。

### 2.1 连接数据库

**方式1：使用Docker进入MySQL容器**
```bash
# 进入MySQL容器
docker exec -it xiaozhi-mysql mysql -uroot -p

# 输入密码（默认：123456）
# 切换到数据库
use xiaozhi;
```

**方式2：使用MySQL客户端**
```bash
mysql -h localhost -P 3306 -u root -p xiaozhi
```

**方式3：使用Navicat等图形化工具**
- 主机：localhost
- 端口：3306
- 用户名：root
- 密码：123456（或你的密码）
- 数据库：xiaozhi

### 2.2 执行SQL脚本

#### SQL 1：添加CMHK ASR/TTS配置（推荐执行）

**文件：** `202512261700.sql`

**作用：** 添加招商局定制的CMHK讯飞ASR和XTTS配置

**执行方法：**

```sql
-- ===============================
-- 1) CMHK 讯飞流式 ASR
-- ===============================

DELETE FROM `ai_model_provider` WHERE id = 'SYSTEM_ASR_CMHKXunfeiStreamASR';
INSERT INTO `ai_model_provider` (
  `id`, `model_type`, `provider_code`, `name`, `fields`, `sort`, `creator`, `create_date`, `updater`, `update_date`
) VALUES (
  'SYSTEM_ASR_CMHKXunfeiStreamASR',
  'ASR',
  'cmhk_xunfei_stream',
  'CMHK讯飞流式语音识别',
  '[{"key":"api_key","label":"API Key","type":"string"},{"key":"gateway_url","label":"网关地址","type":"string"},{"key":"api_path","label":"接口路径","type":"string"},{"key":"biz_id","label":"biz_id","type":"string"},{"key":"app_id","label":"app_id","type":"string"},{"key":"final_wait_timeout","label":"最终句等待秒数","type":"number"},{"key":"output_dir","label":"输出目录","type":"string"}]',
  15,
  1,
  NOW(),
  1,
  NOW()
);

DELETE FROM `ai_model_config` WHERE id = 'ASR_CMHKXunfeiStreamASR';
INSERT INTO `ai_model_config` VALUES (
  'ASR_CMHKXunfeiStreamASR',
  'ASR',
  'CMHKXunfeiStreamASR',
  'CMHK讯飞流式语音识别',
  0,
  1,
  '{"type": "cmhk_xunfei_stream", "api_key": "", "gateway_url": "https://opensseapi.cmhk.com/CMHK-LMMP-PRD_ws_iflytek_ast/CMHK-LMMP-PRD", "api_path": "/tuling/ast/v3", "biz_id": "XiaoZhi", "app_id": "", "final_wait_timeout": 2.0, "output_dir": "tmp/"}',
  NULL,
  NULL,
  15,
  NULL,
  NULL,
  NULL,
  NULL
);

-- ===============================
-- 2) CMHK XTTS
-- ===============================

DELETE FROM `ai_model_provider` WHERE id = 'SYSTEM_TTS_CMHKXTTS';
INSERT INTO `ai_model_provider` (
  `id`, `model_type`, `provider_code`, `name`, `fields`, `sort`, `creator`, `create_date`, `updater`, `update_date`
) VALUES (
  'SYSTEM_TTS_CMHKXTTS',
  'TTS',
  'cmhk_xtts',
  'CMHK讯飞XTTS',
  '[{"key":"api_url","label":"API地址","type":"string"},{"key":"api_key","label":"API Key","type":"string"},{"key":"sample_rate","label":"采样率","type":"number"},{"key":"audio_coding","label":"编码","type":"string"},{"key":"output_dir","label":"输出目录","type":"string"},{"key":"debug","label":"调试日志","type":"boolean"},{"key":"session_param","label":"session_param","type":"dict","dict_name":"session_param"}]',
  24,
  1,
  NOW(),
  1,
  NOW()
);

DELETE FROM `ai_model_config` WHERE id = 'TTS_CMHKXTTS';
INSERT INTO `ai_model_config` VALUES (
  'TTS_CMHKXTTS',
  'TTS',
  'CMHKXTTS',
  'CMHK讯飞XTTS',
  0,
  1,
  '{"type": "cmhk_xtts", "api_url": "https://opensseapi.cmhk.com/CMHK-LMMP-PRD_ws_iflytek_xtts/CMHK-LMMP-PRD/createRec", "api_key": "", "sample_rate": 24000, "audio_coding": "raw", "output_dir": "tmp/", "debug": false, "session_param": {"native_voice_name": "yezi", "read_number": 3, "read_english": 2, "text_type": 1, "speed": 30, "volume": 20, "pitch": 0}}',
  NULL,
  NULL,
  24,
  NULL,
  NULL,
  NULL,
  NULL
);
```

**验证：**
```sql
-- 检查是否添加成功
SELECT * FROM ai_model_provider WHERE provider_code IN ('cmhk_xunfei_stream', 'cmhk_xtts');
SELECT * FROM ai_model_config WHERE id IN ('ASR_CMHKXunfeiStreamASR', 'TTS_CMHKXTTS');
```

---

#### SQL 2：添加阿里百炼ASR和更新豆包配置（可选）

**文件：** `202512301430.sql`

**作用：** 
- 添加阿里百炼Paraformer实时语音识别
- 更新豆包流式ASR配置
- 更新火山引擎TTS多情感参数

**是否需要执行：**
- 如果你需要使用阿里百炼ASR → 执行
- 如果你需要豆包/火山引擎的新功能 → 执行
- 如果不需要这些功能 → 可以不执行

**执行方法：**
```bash
# 方式1：在MySQL中直接执行
source /path/to/202512301430.sql

# 方式2：使用docker复制文件后执行
docker cp 202512301430.sql xiaozhi-mysql:/tmp/
docker exec -it xiaozhi-mysql mysql -uroot -p xiaozhi -e "source /tmp/202512301430.sql"
```

---

#### SQL 3：更新FunASR模型说明（可选）

**文件：** `202601051433.sql`

**作用：** 更新FunASR模型说明文档，将模型改为SenseVoiceSmall

**是否需要执行：**
- 如果你使用FunASR → 执行
- 如果不使用 → 可以不执行

**执行方法：**
```sql
UPDATE `ai_model_config` SET 
`doc_link` = 'https://github.com/modelscope/FunASR/blob/main/runtime/docs/SDK_advanced_guide_online_zh.md',
`remark` = '独立部署FunASR，使用FunASR的API服务，只需要五句话
第一句：mkdir -p ./funasr-runtime-resources/models
第二句：sudo docker run -d -p 10096:10095 --privileged=true -v $PWD/funasr-runtime-resources/models:/workspace/models registry.cn-hangzhou.aliyuncs.com/funasr_repo/funasr:funasr-runtime-sdk-online-cpu-0.1.12
上一句话执行后会进入到容器，继续第三句：cd FunASR/runtime
不要退出容器，继续在容器中执行第四句：nohup bash run_server_2pass.sh --download-model-dir /workspace/models --vad-dir damo/speech_fsmn_vad_zh-cn-16k-common-onnx --model-dir iic/SenseVoiceSmall-onnx  --online-model-dir damo/speech_paraformer-large_asr_nat-zh-cn-16k-common-vocab8404-online-onnx  --punc-dir damo/punc_ct-transformer_zh-cn-common-vad_realtime-vocab272727-onnx --lm-dir damo/speech_ngram_lm_zh-cn-ai-wesp-fst --itn-dir thuduj12/fst_itn_zh --hotword /workspace/models/hotwords.txt > log.txt 2>&1 &
上一句话执行后会进入到容器，继续第五句：tail -f log.txt
第五句话执行完后，会看到模型下载日志，下载完后就可以连接使用了
以上是使用CPU推理，如果有GPU，详细参考：https://github.com/modelscope/FunASR/blob/main/runtime/docs/SDK_advanced_guide_online_zh.md' 
WHERE `id` = 'ASR_FunASRServer';
```

---

## 三、推荐的更新流程

### 3.1 最小更新（只更新代码）

```bash
# 1. 拉取最新镜像
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server

# 2. 重启服务
docker-compose down
docker-compose up -d
```

**优点：**
- 简单快速
- 不影响数据库
- 风险最小

**缺点：**
- 无法使用新增的ASR/TTS配置

---

### 3.2 完整更新（代码+数据库）

```bash
# 1. 备份数据库
docker exec xiaozhi-mysql mysqldump -uroot -p123456 xiaozhi > backup_$(date +%Y%m%d).sql

# 2. 拉取最新镜像
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server

# 3. 执行SQL更新
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi

# 在MySQL中执行SQL 1（CMHK配置）
# 复制粘贴上面的SQL 1内容

# 可选：执行SQL 2和SQL 3

# 4. 重启服务
docker-compose down
docker-compose up -d
```

**优点：**
- 功能完整
- 可以使用所有新功能

**缺点：**
- 需要手动执行SQL
- 稍微复杂一些

---

## 四、使用SQL文件批量执行

### 4.1 准备SQL文件

```bash
# 将SQL文件复制到容器
docker cp main/manager-api/src/main/resources/db/changelog/202512261700.sql xiaozhi-mysql:/tmp/
docker cp main/manager-api/src/main/resources/db/changelog/202512301430.sql xiaozhi-mysql:/tmp/
docker cp main/manager-api/src/main/resources/db/changelog/202601051433.sql xiaozhi-mysql:/tmp/
```

### 4.2 执行SQL文件

```bash
# 执行CMHK配置（推荐）
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi -e "source /tmp/202512261700.sql"

# 执行阿里百炼和豆包更新（可选）
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi -e "source /tmp/202512301430.sql"

# 执行FunASR更新（可选）
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi -e "source /tmp/202601051433.sql"
```

---

## 五、验证更新

### 5.1 检查镜像版本

```bash
# 查看当前运行的镜像
docker ps | grep xiaozhi-server

# 查看镜像详情
docker images | grep xiaozhi-esp32-server
```

### 5.2 检查数据库更新

```bash
# 进入MySQL
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi

# 检查CMHK配置
SELECT * FROM ai_model_provider WHERE provider_code IN ('cmhk_xunfei_stream', 'cmhk_xtts');

# 检查阿里百炼配置
SELECT * FROM ai_model_provider WHERE provider_code = 'aliyunbl_stream';

# 退出
exit
```

### 5.3 检查服务日志

```bash
# 查看服务日志
docker logs -f xiaozhi-server

# 检查是否有错误
docker logs xiaozhi-server 2>&1 | grep -i error
```

---

## 六、常见问题

### 6.1 数据库连接失败

**问题：** 无法连接到MySQL

**解决：**
```bash
# 检查MySQL容器是否运行
docker ps | grep mysql

# 检查MySQL日志
docker logs xiaozhi-mysql

# 重启MySQL
docker restart xiaozhi-mysql
```

### 6.2 SQL执行失败

**问题：** SQL执行报错

**解决：**
```bash
# 检查表是否存在
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi -e "SHOW TABLES;"

# 检查字段是否存在
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi -e "DESC ai_model_provider;"
```

### 6.3 更新后功能不可用

**问题：** 更新后CMHK功能不可用

**检查清单：**
1. 确认SQL已执行成功
2. 确认镜像已更新
3. 确认服务已重启
4. 检查配置文件中的API Key是否正确

---

## 七、回滚方案

### 7.1 回滚镜像

```bash
# 使用旧版本镜像
docker-compose down
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server-old-version
docker-compose up -d
```

### 7.2 回滚数据库

```bash
# 恢复备份
docker exec -i xiaozhi-mysql mysql -uroot -p123456 xiaozhi < backup_20260126.sql
```

---

## 八、总结

### 推荐方案

**如果你需要CMHK功能：**
1. 拉取最新镜像
2. 执行SQL 1（202512261700.sql）
3. 重启服务

**如果你只需要代码更新：**
1. 拉取最新镜像
2. 重启服务
3. 不执行任何SQL

**如果你需要所有新功能：**
1. 备份数据库
2. 拉取最新镜像
3. 执行所有SQL（1、2、3）
4. 重启服务

---

**文档生成时间：** 2026-01-26  
**适用版本：** 0.8.11  
**状态：** ✅ 完整指南
