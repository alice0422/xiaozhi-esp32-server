# 镜像 vs 容器 说明

## 🔍 关键区别

### `docker images` = 查看镜像（静态模板）
- **镜像** = 软件安装包（.exe文件）
- 存储在磁盘上，不会自动运行
- 可以创建多个容器

### `docker ps` = 查看容器（运行中的实例）
- **容器** = 运行起来的程序
- 需要从镜像创建并启动
- 只有运行中的容器才会显示

## 📊 你的情况

```bash
docker images  # ✅ 有镜像（模板存在）
docker ps      # ❌ 没有容器（没有运行中的实例）
```

**原因：**
- 镜像已经构建好了（模板准备好了）
- 但还没有从镜像创建并启动容器（程序还没运行）

## 🚀 解决方案：启动容器

### 步骤1：检查是否有停止的容器

```bash
# 查看所有容器（包括停止的）
docker ps -a
```

如果看到停止的容器，可以：
```bash
# 启动已存在的容器
docker start <容器名或ID>

# 或者删除后重新创建
docker rm <容器名或ID>
```

### 步骤2：使用 docker-compose 启动（推荐）

```bash
cd main/xiaozhi-server

# 检查配置文件是否使用本地镜像
cat docker-compose_all.yml | grep image

# 启动所有服务
docker-compose -f docker-compose_all.yml up -d
```

### 步骤3：验证容器运行

```bash
# 查看运行中的容器
docker ps

# 应该看到：
# - xiaozhi-esp32-server
# - xiaozhi-esp32-server-web
# - xiaozhi-esp32-server-db
# - xiaozhi-esp32-server-redis
```

## 📝 完整流程

### 1. 确保 docker-compose 使用本地镜像

编辑 `main/xiaozhi-server/docker-compose_all.yml`：

```yaml
xiaozhi-esp32-server:
  image: xiaozhi-esp32-server:server_latest  # 使用本地镜像

xiaozhi-esp32-server-web:
  image: xiaozhi-esp32-server:web_latest     # 使用本地镜像（如果已构建）
```

### 2. 启动服务

```bash
cd main/xiaozhi-server
docker-compose -f docker-compose_all.yml up -d
```

### 3. 查看状态

```bash
# 查看容器状态
docker ps

# 查看日志
docker logs xiaozhi-esp32-server
docker logs xiaozhi-esp32-server-web
```

## 🔄 镜像 → 容器的关系

```
镜像 (Image)         容器 (Container)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📦 模板/安装包        🏃 运行中的程序
📋 存储在磁盘         💾 在内存中运行
🔄 可以创建多个容器   ⚡ 从镜像创建
❌ 不会自动运行       ✅ 需要启动才会运行
```

## ⚠️ 常见问题

### Q: 为什么有镜像但没有容器？
A: 镜像只是模板，需要创建并启动容器才会运行。

### Q: 如何从镜像创建容器？
A: 使用 `docker run` 或 `docker-compose up`。

### Q: 容器停止后镜像还在吗？
A: 在。镜像和容器是分开的，删除容器不会删除镜像。

### Q: 如何查看所有容器（包括停止的）？
A: 使用 `docker ps -a`。

## 🎯 下一步操作

1. **检查配置文件**：确保使用本地镜像
2. **启动容器**：使用 docker-compose
3. **验证运行**：查看 docker ps 和日志


















