# 数据安全说明 - 更新不会丢失数据

## 重要结论

**✅ 拉取镜像和重启服务不会清除数据库！**

---

## 为什么数据不会丢失？

### 1. 数据库使用了Volume挂载

查看你的docker-compose_all.yml：

```yaml
xiaozhi-esp32-server-db:
  image: mysql:latest
  container_name: xiaozhi-esp32-server-db
  volumes:
    - ./mysql/data:/var/lib/mysql  # ← 数据持久化到本地
  environment:
    - MYSQL_ROOT_PASSWORD=chatglm.cn
    - MYSQL_DATABASE=xiaozhi_esp32_server
```

**关键点：**
- 数据库文件存储在：`./mysql/data`（你的本地目录）
- 容器内路径：`/var/lib/mysql`
- 即使删除容器，本地的`./mysql/data`目录不会被删除
- 重启容器时，会重新挂载这个目录，数据完整保留

---

## 各种操作对数据的影响

### ✅ 安全操作（不会丢失数据）

| 操作 | 命令 | 数据影响 | 说明 |
|------|------|---------|------|
| **拉取镜像** | `docker compose pull` | ✅ 无影响 | 只下载镜像，不影响容器和数据 |
| **重启服务** | `docker compose restart` | ✅ 无影响 | 容器重启，数据保留 |
| **停止并启动** | `docker compose down && up -d` | ✅ 无影响 | 容器重建，但数据volume保留 |
| **更新镜像** | `pull && up -d` | ✅ 无影响 | 使用新镜像，数据volume保留 |

### ⚠️ 需要注意的操作

| 操作 | 命令 | 数据影响 | 说明 |
|------|------|---------|------|
| **删除volume** | `docker compose down -v` | ❌ 会删除 | `-v`参数会删除volume |
| **删除数据目录** | `rm -rf ./mysql/data` | ❌ 会删除 | 直接删除本地数据 |

---

## 实际测试验证

### 测试1：拉取镜像

```bash
# 拉取镜像前
docker compose -f docker-compose_all.yml exec xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn -e "SELECT COUNT(*) FROM xiaozhi_esp32_server.ai_model_config;"
# 输出：假设有50条记录

# 拉取镜像
docker compose -f docker-compose_all.yml pull xiaozhi-esp32-server-web

# 拉取镜像后（数据不变）
docker compose -f docker-compose_all.yml exec xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn -e "SELECT COUNT(*) FROM xiaozhi_esp32_server.ai_model_config;"
# 输出：还是50条记录
```

### 测试2：重启服务

```bash
# 重启前
docker compose -f docker-compose_all.yml exec xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn -e "SELECT COUNT(*) FROM xiaozhi_esp32_server.ai_model_config;"
# 输出：50条记录

# 重启服务
docker compose -f docker-compose_all.yml restart xiaozhi-esp32-server-web

# 重启后（数据不变）
docker compose -f docker-compose_all.yml exec xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn -e "SELECT COUNT(*) FROM xiaozhi_esp32_server.ai_model_config;"
# 输出：还是50条记录
```

### 测试3：停止并重新启动

```bash
# 停止所有服务
docker compose -f docker-compose_all.yml down

# 重新启动
docker compose -f docker-compose_all.yml up -d

# 检查数据（数据不变）
docker compose -f docker-compose_all.yml exec xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn -e "SELECT COUNT(*) FROM xiaozhi_esp32_server.ai_model_config;"
# 输出：还是50条记录
```

---

## 数据存储位置

### 查看数据目录

```bash
# 进入xiaozhi-server目录
cd main/xiaozhi-server

# 查看MySQL数据目录
ls -lh ./mysql/data/

# 输出示例：
# drwxr-x--- 2 999 docker 4.0K Jan 26 10:00 xiaozhi_esp32_server/
# -rw-r----- 1 999 docker  56M Jan 26 10:00 ib_logfile0
# -rw-r----- 1 999 docker  76M Jan 26 10:00 ibdata1
# ...
```

### 数据目录大小

```bash
# 查看数据库占用空间
du -sh ./mysql/data/
# 输出示例：500M    ./mysql/data/
```

**这些文件都在你的本地磁盘上，不会因为容器操作而丢失！**

---

## SQL迁移的工作原理

### Liquibase如何工作

1. **首次启动**
   - manager-api启动时，Liquibase检查数据库
   - 创建`DATABASECHANGELOG`表（记录已执行的SQL）
   - 执行所有未执行的SQL迁移脚本

2. **后续启动**
   - 检查`DATABASECHANGELOG`表
   - 只执行新的、未执行过的SQL
   - 已执行过的SQL不会重复执行

3. **数据安全**
   - SQL迁移只是**添加**新配置
   - 不会**删除**现有数据
   - 不会**清空**表

### 查看已执行的SQL

```bash
docker exec -it xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn xiaozhi_esp32_server -e "SELECT * FROM DATABASECHANGELOG ORDER BY DATEEXECUTED DESC LIMIT 10;"
```

输出示例：
```
+----+------------------+---------------------+
| ID | FILENAME         | DATEEXECUTED        |
+----+------------------+---------------------+
| 1  | 202512261700.sql | 2026-01-26 10:00:00 |
| 2  | 202512301430.sql | 2026-01-26 10:00:01 |
| 3  | 202601051433.sql | 2026-01-26 10:00:02 |
+----+------------------+---------------------+
```

---

## 什么情况下会丢失数据？

### ❌ 危险操作1：使用-v参数

```bash
# 这个命令会删除volume！
docker compose -f docker-compose_all.yml down -v

# 正确的命令（不删除volume）
docker compose -f docker-compose_all.yml down
```

### ❌ 危险操作2：删除数据目录

```bash
# 这个命令会删除所有数据！
rm -rf ./mysql/data/

# 千万不要执行！
```

### ❌ 危险操作3：删除容器和volume

```bash
# 这个命令会删除容器和volume！
docker rm -v xiaozhi-esp32-server-db

# 正确的命令（只删除容器）
docker rm xiaozhi-esp32-server-db
```

---

## 安全更新的完整流程

### 推荐流程（最安全）

```bash
# 1. 备份数据库（可选但推荐）
cd main/xiaozhi-server
docker exec xiaozhi-esp32-server-db mysqldump -uroot -pchatglm.cn xiaozhi_esp32_server > backup_$(date +%Y%m%d_%H%M%S).sql

# 2. 拉取最新镜像
docker compose -f docker-compose_all.yml pull

# 3. 重启服务
docker compose -f docker-compose_all.yml up -d

# 4. 验证数据完整性
docker compose -f docker-compose_all.yml exec xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn -e "SELECT COUNT(*) FROM xiaozhi_esp32_server.ai_model_config;"
```

### 快速流程（也安全）

```bash
cd main/xiaozhi-server && docker compose -f docker-compose_all.yml pull && docker compose -f docker-compose_all.yml up -d
```

---

## 数据备份建议

### 定期备份

```bash
# 创建备份脚本
cat > backup.sh << 'EOF'
#!/bin/bash
BACKUP_DIR="./backups"
mkdir -p $BACKUP_DIR
DATE=$(date +%Y%m%d_%H%M%S)
docker exec xiaozhi-esp32-server-db mysqldump -uroot -pchatglm.cn xiaozhi_esp32_server > $BACKUP_DIR/backup_$DATE.sql
echo "备份完成: $BACKUP_DIR/backup_$DATE.sql"
EOF

chmod +x backup.sh

# 执行备份
./backup.sh
```

### 恢复备份

```bash
# 如果需要恢复
docker exec -i xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn xiaozhi_esp32_server < ./backups/backup_20260126_100000.sql
```

---

## 总结

### ✅ 放心操作

以下操作**完全安全**，不会丢失数据：

1. `docker compose pull` - 拉取镜像
2. `docker compose restart` - 重启服务
3. `docker compose down && up -d` - 停止并启动
4. `docker compose up -d` - 更新服务

### ⚠️ 注意事项

1. **不要使用** `docker compose down -v`（会删除volume）
2. **不要删除** `./mysql/data/` 目录
3. **建议定期备份**数据库

### 🎯 核心原理

**数据存储在本地目录，容器只是访问这个目录，所以：**
- 容器可以随意删除和重建
- 镜像可以随意更新
- 数据永远保存在本地磁盘

---

**文档生成时间：** 2026-01-26  
**结论：** ✅ 更新镜像和重启服务完全安全，不会丢失数据！
