# manager-mobile 项目说明

> **小智AI移动端管理后台** - 跨平台移动管理应用

---

## 📱 项目定位

**manager-mobile** 是小智AI系统的**移动端管理后台**，让管理员可以在手机、平板等移动设备上管理小智AI系统。

**一句话概括：**
> 把PC端的智控台（manager-web）搬到手机上，支持iOS、Android、微信小程序。

---

## 🎯 核心价值

### 1. 跨平台支持
- ✅ **一套代码，多端运行**
- ✅ 支持 **H5**、**iOS App**、**Android App**、**微信小程序**
- ✅ 无需为每个平台单独开发

### 2. 功能完整
- ✅ 设备管理（绑定、解绑、配置）
- ✅ 智能体配置（创建、编辑、管理）
- ✅ 聊天历史查看
- ✅ 声纹识别管理
- ✅ 系统设置

### 3. 用户体验
- ✅ 移动端优化界面
- ✅ 支持多语言（中文、英文、德文、越南文等）
- ✅ 响应式设计，适配不同屏幕

---

## 🛠️ 技术架构

### 技术栈

| 技术 | 说明 |
|------|------|
| **uni-app v3** | 跨平台框架，一套代码多端运行 |
| **Vue 3** | 前端框架 |
| **Vite** | 构建工具，开发体验好 |
| **TypeScript** | 类型安全 |
| **Pinia** | 状态管理 |
| **UnoCSS** | 原子化CSS，样式开发快 |
| **alova** | 网络请求库，适配uni-app |

### 项目结构

```
manager-mobile/
├── src/
│   ├── pages/          # 页面
│   │   ├── device/     # 设备管理
│   │   ├── agent/      # 智能体配置
│   │   ├── chat-history/ # 聊天历史
│   │   ├── voiceprint/  # 声纹识别
│   │   └── settings/    # 系统设置
│   ├── api/            # API接口封装
│   ├── store/          # 状态管理
│   ├── components/     # 组件
│   ├── layouts/        # 布局
│   └── utils/          # 工具函数
├── env/                # 环境变量配置
└── manifest.config.ts  # 应用配置
```

---

## 📋 主要功能

### 1. 设备管理
- **设备列表**：查看已绑定的设备
- **设备绑定**：扫描二维码或输入绑定码绑定设备
- **设备解绑**：解除设备绑定
- **设备配置**：WiFi配网、超声波配置等
- **固件更新**：OTA固件升级管理

### 2. 智能体配置
- **智能体列表**：查看所有智能体
- **创建/编辑**：创建新的智能体或编辑现有智能体
- **模型配置**：配置ASR、LLM、TTS等模型
- **工具管理**：管理智能体可用的工具/插件

### 3. 聊天历史
- **历史记录**：查看用户与设备的对话历史
- **详情查看**：查看单次对话的详细信息

### 4. 声纹识别
- **声纹管理**：注册、删除声纹
- **说话人识别**：查看识别记录

### 5. 系统设置
- **用户信息**：修改个人信息
- **语言设置**：切换界面语言
- **其他设置**：系统相关配置

---

## 🚀 开发与部署

### 开发环境要求
- Node.js >= 18
- pnpm >= 7.30（推荐使用 pnpm@10.x）
- HBuilderX（App调试/打包）
- 微信开发者工具（微信小程序调试）

### 快速开始

1. **安装依赖**
```bash
pnpm i
```

2. **配置环境变量**
- 复制 `env/.env.example` 为 `env/.env.development`
- 修改配置项：
  - `VITE_SERVER_BASEURL`：后端API地址
  - `VITE_UNI_APPID`：uni-app应用ID（App用）
  - `VITE_WX_APPID`：微信小程序ID（小程序用）

3. **本地开发**
```bash
# H5开发
pnpm dev:h5

# 微信小程序开发
pnpm dev:mp-weixin

# App开发（需用HBuilderX）
pnpm dev:app
```

### 构建与发布

**微信小程序：**
```bash
pnpm build:mp-weixin
# 产物在 dist/build/mp-weixin
# 用微信开发者工具导入并上传
```

**App（Android/iOS）：**
```bash
pnpm build:app
# 产物在 dist/build/app
# 用HBuilderX导入，进行云打包
```

---

## 🔗 与系统其他组件的关系

```
小智AI系统架构：

┌─────────────────┐
│  ESP32设备      │
│  (硬件终端)     │
└────────┬────────┘
         │ WebSocket
         │
┌────────▼────────┐
│ xiaozhi-server │
│ (Python后端)   │
└────────┬────────┘
         │ HTTP API
         │
┌────────▼────────┐      ┌──────────────────┐
│  manager-api    │◄─────┤  manager-web     │
│  (Java后端)     │      │  (PC管理后台)    │
└────────┬────────┘      └──────────────────┘
         │ HTTP API
         │
┌────────▼────────┐
│ manager-mobile │
│ (移动管理端)   │
└─────────────────┘
```

**关系说明：**
- `manager-mobile` 和 `manager-web` 都是管理后台
- 两者都通过 HTTP API 调用 `manager-api`
- `manager-api` 提供统一的RESTful API接口
- 移动端和PC端功能基本一致，但界面适配不同平台

---

## 💡 销售与SA要点

### 对销售说：

> "我们提供移动端管理应用，管理员可以在手机上管理小智AI系统。支持iOS、Android、微信小程序，一套代码多端运行，开发成本低。功能包括设备管理、智能体配置、聊天历史查看等，和PC端功能基本一致。"

### 对SA说：

**技术优势：**
- ✅ 跨平台框架，维护成本低
- ✅ 与PC端共用同一套API，无需额外开发
- ✅ 支持多语言，国际化友好
- ✅ 响应式设计，适配不同屏幕

**部署建议：**
- **微信小程序**：适合快速上线，无需应用商店审核
- **App**：适合需要更多权限和功能的场景
- **H5**：适合内嵌到其他系统

**定制开发：**
- 可以定制UI风格，符合客户品牌
- 可以添加客户特定的功能模块
- 可以对接客户的业务系统

---

## 📊 功能对比

| 功能 | PC端(manager-web) | 移动端(manager-mobile) |
|------|------------------|----------------------|
| **设备管理** | ✅ | ✅ |
| **智能体配置** | ✅ | ✅ |
| **聊天历史** | ✅ | ✅ |
| **声纹识别** | ✅ | ✅ |
| **用户管理** | ✅ | ⚠️ 部分功能 |
| **系统配置** | ✅ | ⚠️ 部分功能 |
| **平台支持** | Web浏览器 | iOS/Android/微信小程序/H5 |

---

## ⚠️ 注意事项

### 部署前必须配置：

1. **应用ID**
   - `VITE_UNI_APPID`：需要在DCloud开发者中心创建应用
   - `VITE_WX_APPID`：需要在微信公众平台注册小程序

2. **服务端地址**
   - `VITE_SERVER_BASEURL`：修改为实际的后端API地址

3. **应用信息**
   - `VITE_APP_TITLE`：修改应用名称
   - 更新应用图标和启动图

### 平台差异：

- **微信小程序**：有严格的审核流程，功能受限
- **App**：需要应用商店审核，但功能更完整
- **H5**：无需审核，但功能受限（如无法调用摄像头）

---

## 📝 总结

**manager-mobile** 是小智AI系统的移动端管理后台，采用uni-app跨平台框架，一套代码支持iOS、Android、微信小程序、H5多个平台。功能与PC端基本一致，让管理员可以随时随地管理小智AI系统。

**核心优势：**
- ✅ 跨平台，开发成本低
- ✅ 功能完整，体验好
- ✅ 与PC端共用API，维护简单
- ✅ 支持多语言，国际化友好

**适用场景：**
- 管理员需要移动端管理
- 客户希望有微信小程序版本
- 需要快速上线移动端应用

