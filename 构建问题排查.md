# Docker构建失败 - Exit Code 137 解决方案

## 🔴 问题原因

**Exit Code 137** = 进程被系统强制终止（SIGKILL）
- 最常见原因：**内存不足（OOM - Out of Memory）**
- 系统内存耗尽，OOM killer 杀死了构建进程

## 🔍 诊断步骤

### 1. 检查服务器内存
```bash
# 查看内存使用情况
free -h

# 查看系统日志中的OOM记录
dmesg | tail -50 | grep -i "out of memory"
dmesg | grep -i "killed process"
```

### 2. 检查Docker资源
```bash
# 查看Docker资源使用
docker system df

# 查看正在运行的容器内存使用
docker stats --no-stream
```

### 3. 检查Swap空间
```bash
# 查看swap空间
swapon --show
free -h
```

## ✅ 解决方案

### 方案1：增加Swap空间（推荐，最简单）

```bash
# 创建2GB swap文件
sudo fallocate -l 2G /swapfile
# 或者如果fallocate不支持，用dd：
# sudo dd if=/dev/zero of=/swapfile bs=1024 count=2097152

# 设置权限
sudo chmod 600 /swapfile

# 格式化为swap
sudo mkswap /swapfile

# 启用swap
sudo swapon /swapfile

# 永久启用（重启后仍然有效）
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab

# 验证
free -h
```

### 方案2：使用优化版Dockerfile

使用 `Dockerfile-server-base-optimized`，分批安装依赖：

```bash
docker build -t xiaozhi-esp32-server:server-base -f ./Dockerfile-server-base-optimized .
```

### 方案3：清理系统资源

```bash
# 停止不必要的容器
docker ps
docker stop <container_id>

# 清理Docker缓存和未使用的资源
docker system prune -a

# 清理未使用的镜像
docker image prune -a
```

### 方案4：限制构建并发（如果使用buildx）

```bash
# 使用单线程构建，减少内存占用
docker buildx build --builder default --load -t xiaozhi-esp32-server:server-base -f ./Dockerfile-server-base .
```

### 方案5：增加服务器内存（长期方案）

如果服务器内存确实太小（<2GB），考虑：
- 升级服务器配置
- 使用更大内存的服务器
- 使用云服务器的弹性扩容

## 🚀 推荐操作流程

### 步骤1：立即增加Swap
```bash
sudo fallocate -l 2G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile
free -h  # 验证swap已启用
```

### 步骤2：清理Docker资源
```bash
docker system prune -f
```

### 步骤3：使用优化版Dockerfile重新构建
```bash
# 修改构建脚本，使用优化版Dockerfile
docker build -t xiaozhi-esp32-server:server-base -f ./Dockerfile-server-base-optimized .
```

### 步骤4：监控构建过程
```bash
# 另开一个终端，监控内存使用
watch -n 1 free -h

# 或者监控Docker进程
docker stats
```

## 📊 内存需求参考

构建 `server-base` 镜像的典型内存需求：
- **最低要求**：2GB RAM + 2GB Swap
- **推荐配置**：4GB RAM
- **Python包安装阶段**：峰值可能达到 3-4GB

## ⚠️ 注意事项

1. **Swap会影响性能**：使用swap会变慢，但能避免OOM
2. **构建时间会变长**：使用swap后，构建可能需要更长时间
3. **完成后可以删除swap**（如果不需要）：
   ```bash
   sudo swapoff /swapfile
   sudo rm /swapfile
   sudo sed -i '/swapfile/d' /etc/fstab
   ```

## 🔧 快速修复命令（一键执行）

```bash
# 创建并启用2GB swap
sudo fallocate -l 2G /swapfile && \
sudo chmod 600 /swapfile && \
sudo mkswap /swapfile && \
sudo swapon /swapfile && \
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab && \
free -h
```

执行后，重新运行构建脚本即可。









