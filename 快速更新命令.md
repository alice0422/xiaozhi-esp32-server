# 快速更新命令

## 使用docker-compose_all.yml更新

### 方法1：更新单个服务

```bash
# 进入目录
cd main/xiaozhi-server

# 拉取server镜像
docker compose -f docker-compose_all.yml pull xiaozhi-esp32-server

# 拉取web镜像（包含manager-api，会自动执行SQL）
docker compose -f docker-compose_all.yml pull xiaozhi-esp32-server-web

# 重启服务
docker compose -f docker-compose_all.yml up -d xiaozhi-esp32-server
docker compose -f docker-compose_all.yml up -d xiaozhi-esp32-server-web
```

### 方法2：更新所有服务（推荐）

```bash
# 进入目录
cd main/xiaozhi-server

# 拉取所有镜像
docker compose -f docker-compose_all.yml pull

# 重启所有服务
docker compose -f docker-compose_all.yml up -d
```

### 方法3：一条命令搞定

```bash
cd main/xiaozhi-server && docker compose -f docker-compose_all.yml pull && docker compose -f docker-compose_all.yml up -d
```

---

## 验证更新

### 检查镜像版本

```bash
docker images | grep xiaozhi-esp32-server
```

### 检查服务状态

```bash
docker compose -f docker-compose_all.yml ps
```

### 查看日志

```bash
# 查看server日志
docker compose -f docker-compose_all.yml logs -f xiaozhi-esp32-server

# 查看web日志（包含SQL执行记录）
docker compose -f docker-compose_all.yml logs -f xiaozhi-esp32-server-web

# 搜索SQL执行记录
docker compose -f docker-compose_all.yml logs xiaozhi-esp32-server-web | grep "202512261700"
```

---

## 注意事项

### 1. 镜像源

你的docker-compose_all.yml使用的是：
```yaml
image: ghcr.io/alice0422/xiaozhi-esp32-server:server
image: ghcr.io/alice0422/xiaozhi-esp32-server:web
```

这是你自己的镜像仓库（alice0422），所以：
- ✅ 可以直接pull
- ✅ 会拉取你刚才推送的最新代码
- ✅ 包含所有恢复的功能（CMHK、音乐、歌词等）

### 2. 数据库密码

你的配置中数据库密码是：`chatglm.cn`

如果需要手动连接数据库：
```bash
docker exec -it xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn xiaozhi_esp32_server
```

### 3. SQL自动执行

当你重启`xiaozhi-esp32-server-web`服务时，manager-api会自动：
1. 检查数据库版本
2. 执行未执行过的SQL迁移脚本
3. 记录到`DATABASECHANGELOG`表

---

## 完整更新流程（推荐）

```bash
# 1. 进入目录
cd main/xiaozhi-server

# 2. 备份数据库（可选但推荐）
docker exec xiaozhi-esp32-server-db mysqldump -uroot -pchatglm.cn xiaozhi_esp32_server > backup_$(date +%Y%m%d_%H%M%S).sql

# 3. 拉取最新镜像
docker compose -f docker-compose_all.yml pull

# 4. 重启服务
docker compose -f docker-compose_all.yml up -d

# 5. 查看日志确认
docker compose -f docker-compose_all.yml logs -f xiaozhi-esp32-server-web
```

---

## 如果遇到问题

### 问题1：拉取镜像慢

**解决方案：使用国内镜像加速**

修改docker-compose_all.yml：
```yaml
xiaozhi-esp32-server:
  image: ghcr.nju.edu.cn/alice0422/xiaozhi-esp32-server:server
  
xiaozhi-esp32-server-web:
  image: ghcr.nju.edu.cn/alice0422/xiaozhi-esp32-server:web
```

### 问题2：服务启动失败

**检查步骤：**
```bash
# 查看服务状态
docker compose -f docker-compose_all.yml ps

# 查看错误日志
docker compose -f docker-compose_all.yml logs xiaozhi-esp32-server
docker compose -f docker-compose_all.yml logs xiaozhi-esp32-server-web

# 重启服务
docker compose -f docker-compose_all.yml restart
```

### 问题3：SQL没有自动执行

**手动执行：**
```bash
# 进入web容器
docker exec -it xiaozhi-esp32-server-web bash

# 查看SQL文件是否存在
ls -la /app/BOOT-INF/classes/db/changelog/

# 手动执行SQL
docker exec -it xiaozhi-esp32-server-db mysql -uroot -pchatglm.cn xiaozhi_esp32_server -e "source /tmp/202512261700.sql"
```

---

## 总结

**最简单的更新命令：**
```bash
cd main/xiaozhi-server && docker compose -f docker-compose_all.yml pull && docker compose -f docker-compose_all.yml up -d
```

**这一条命令会：**
1. ✅ 拉取最新的server镜像（包含CMHK、音乐、歌词功能）
2. ✅ 拉取最新的web镜像（包含manager-api）
3. ✅ 重启所有服务
4. ✅ 自动执行数据库迁移SQL

**不需要任何手动操作！**

---

**文档生成时间：** 2026-01-26  
**适用版本：** 0.8.11  
**镜像仓库：** ghcr.io/alice0422/xiaozhi-esp32-server
