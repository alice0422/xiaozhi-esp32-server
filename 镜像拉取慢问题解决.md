# 镜像拉取慢问题解决方案

## 🔍 问题现象

执行 `docker-compose up -d` 时，镜像拉取非常慢（超过 10 分钟），可能的原因：

1. **未登录 GitHub Container Registry**
2. **网络问题**（国内访问 GitHub 可能很慢）
3. **镜像文件较大**

## ✅ 解决方案

### 方案 1：先登录再拉取（推荐）

**重要**：使用 GitHub Container Registry 的镜像前，必须先登录！

```bash
# 1. 创建 GitHub Personal Access Token
# 访问：https://github.com/settings/tokens
# 创建 Token，勾选 read:packages 权限

# 2. 登录到 GitHub Container Registry
echo "YOUR_GITHUB_TOKEN" | docker login ghcr.io -u alice0422 --password-stdin

# 3. 验证登录
docker login ghcr.io

# 4. 手动拉取镜像（可以看到进度）
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:web

# 5. 启动服务
cd /opt/xiaozhi-esp32-server/main/xiaozhi-server
docker-compose -f docker-compose_all.yml up -d
```

### 方案 2：使用镜像加速器（国内用户推荐）

如果登录后仍然很慢，可以使用镜像加速器：

#### 方法 A：配置 Docker 镜像加速器

编辑 `/etc/docker/daemon.json`：

```json
{
  "registry-mirrors": [
    "https://docker.mirrors.ustc.edu.cn",
    "https://hub-mirror.c.163.com"
  ]
}
```

重启 Docker：
```bash
sudo systemctl daemon-reload
sudo systemctl restart docker
```

**注意**：GitHub Container Registry 可能不支持镜像加速，但可以加速其他基础镜像。

#### 方法 B：使用代理

如果有代理，可以配置 Docker 使用代理：

```bash
# 创建代理配置目录
sudo mkdir -p /etc/systemd/system/docker.service.d

# 创建代理配置文件
sudo tee /etc/systemd/system/docker.service.d/http-proxy.conf <<EOF
[Service]
Environment="HTTP_PROXY=http://proxy.example.com:8080"
Environment="HTTPS_PROXY=http://proxy.example.com:8080"
Environment="NO_PROXY=localhost,127.0.0.1"
EOF

# 重启 Docker
sudo systemctl daemon-reload
sudo systemctl restart docker
```

### 方案 3：分步拉取，查看进度

```bash
# 1. 先单独拉取镜像，可以看到详细进度
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server

# 2. 如果拉取成功，再拉取 web 镜像
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:web

# 3. 验证镜像是否存在
docker images | grep xiaozhi-esp32-server

# 4. 启动服务（此时不会重新拉取）
docker-compose -f docker-compose_all.yml up -d
```

### 方案 4：使用本地构建的镜像（如果网络太慢）

如果 GitHub 镜像拉取太慢，可以临时使用本地构建的镜像：

```yaml
# 临时修改 docker-compose_all.yml
image: xiaozhi-esp32-server:server_latest  # 使用本地镜像
image: xiaozhi-esp32-server:web_latest    # 使用本地镜像
```

然后本地构建：
```bash
# 在项目根目录
docker build -t xiaozhi-esp32-server:server_latest -f Dockerfile-server-local .
docker build -t xiaozhi-esp32-server:web_latest -f Dockerfile-web .
```

## 🔍 检查镜像大小

镜像可能很大，检查一下：

```bash
# 查看镜像大小
docker images ghcr.io/alice0422/xiaozhi-esp32-server

# 或者查看所有镜像
docker images | grep xiaozhi
```

如果镜像很大（> 1GB），拉取时间会较长。

## 📊 诊断步骤

### 1. 检查是否已登录

```bash
# 检查登录状态
cat ~/.docker/config.json | grep ghcr.io
```

如果没有 `ghcr.io`，说明未登录。

### 2. 测试网络连接

```bash
# 测试能否访问 GitHub Container Registry
curl -I https://ghcr.io

# 或者测试拉取一个小镜像
docker pull hello-world
```

### 3. 查看拉取日志

```bash
# 使用详细模式拉取，查看具体错误
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server --progress=plain
```

## ⚡ 快速解决步骤

```bash
# 1. 登录（必须！）
echo "YOUR_GITHUB_TOKEN" | docker login ghcr.io -u alice0422 --password-stdin

# 2. 测试拉取（查看是否能正常拉取）
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server

# 3. 如果拉取成功，启动服务
cd /opt/xiaozhi-esp32-server/main/xiaozhi-server
docker-compose -f docker-compose_all.yml up -d
```

## 🎯 推荐流程

1. **先登录** - 这是最重要的步骤
2. **单独拉取** - 先手动拉取镜像，确认能成功
3. **再启动** - 镜像拉取成功后，再启动服务

## 📝 注意事项

- ✅ **必须登录**：未登录会导致拉取失败或非常慢
- ✅ **网络问题**：国内访问 GitHub 可能很慢，需要耐心等待
- ✅ **镜像大小**：镜像可能很大，首次拉取需要较长时间
- ✅ **Token 权限**：确保 Token 有 `read:packages` 权限

如果登录后仍然很慢，建议：
1. 使用代理（如果有）
2. 或者先本地构建镜像使用











