# 豆包/火山引擎ASR/TTS对比：0.8.8 vs 0.8.11

## 生成时间
2026-01-26

---

## 一、核心结论

### 豆包ASR（doubao_stream.py）

**❌ 不完全一致 - 0.8.11有重要更新**

### 火山引擎TTS（huoshan_double_stream.py）

**✅ 完全一致 - 代码相同**

---

## 二、豆包ASR详细差异

### 2.1 多语种支持（重要新功能）

**0.8.8：**
```python
# 固定使用单一语种模式
self.ws_url = "wss://openspeech.bytedance.com/api/v3/sauc/bigmodel"
self.language = config.get("language", "zh-CN")
```

**0.8.11：**
```python
# 支持多语种模式切换
enable_multilingual = config.get("enable_multilingual", False)
self.enable_multilingual = False if str(enable_multilingual).lower() == 'false' else True

if self.enable_multilingual:
    self.ws_url = "wss://openspeech.bytedance.com/api/v3/sauc/bigmodel_nostream"
else:
    self.ws_url = "wss://openspeech.bytedance.com/api/v3/sauc/bigmodel"

# language参数仅在多语种模式下有效
self.language = config.get("language") if self.enable_multilingual else None
```

**差异说明：**
- 0.8.11新增 `enable_multilingual` 参数
- 多语种模式使用不同的WebSocket地址（bigmodel_nostream）
- language参数仅在多语种模式下生效

### 2.2 请求参数构造差异

**0.8.8：**
```python
def construct_request(self, reqid):
    req = {
        "app": {
            "appid": self.appid,
            "cluster": self.cluster,
            "token": self.access_token,
        },
        "user": {"uid": self.uid},
        "request": {
            "reqid": reqid,
            "workflow": self.workflow,
            "show_utterances": True,
            "result_type": self.result_type,
            "sequence": 1,
            "boosting_table_name": self.boosting_table_name,
            "correct_table_name": self.correct_table_name,
            "end_window_size": self.end_window_size,
        },
        "audio": {
            "format": self.format,
            "codec": self.codec,
            "rate": self.rate,
            "language": self.language,  # ← 总是包含language
            "bits": self.bits,
            "channel": self.channel,
            "sample_rate": self.rate,
        },
    }
    return req
```

**0.8.11：**
```python
def construct_request(self, reqid):
    req = {
        "app": {
            "appid": self.appid,
            "cluster": self.cluster,
            "token": self.access_token,
        },
        "user": {"uid": self.uid},
        "request": {
            "reqid": reqid,
            "workflow": self.workflow,
            "show_utterances": True,
            "result_type": self.result_type,
            "sequence": 1,
            "boosting_table_name": self.boosting_table_name,
            "correct_table_name": self.correct_table_name,
            "end_window_size": self.end_window_size,
        },
        "audio": {
            "format": self.format,
            "codec": self.codec,
            "rate": self.rate,
            "bits": self.bits,
            "channel": self.channel,
            "sample_rate": self.rate,
        },
    }

    # ✅ language参数仅在多语种模式下添加
    if self.enable_multilingual and self.language:
        req["audio"]["language"] = self.language

    return req
```

**差异说明：**
- 0.8.8总是包含language参数
- 0.8.11只在多语种模式下才添加language参数
- 这是为了兼容不同的API模式

### 2.3 结果处理差异

**0.8.8：**
```python
# 处理识别结果
for utterance in utterances:
    if utterance.get("definite", False):
        current_text = utterance["text"]
        # ... 处理逻辑
```

**0.8.11：**
```python
# 多语种模式下跳过某些处理
if self.enable_multilingual:
    continue

# 检查停止信号
if conn.client_listen_mode == "manual" and conn.client_voice_stop and len(audio_data) > 0:
    logger.bind(tag=TAG).debug("消息结束收到停止信号，触发处理")
    await self.handle_voice_stop(conn, audio_data)
    conn.asr_audio.clear()
    conn.reset_vad_states()
    break

# 处理识别结果
for utterance in utterances:
    if utterance.get("definite", False):
        current_text = utterance["text"]
        # ... 处理逻辑
```

**差异说明：**
- 0.8.11新增多语种模式的特殊处理
- 0.8.11改进了停止信号的处理逻辑

---

## 三、火山引擎TTS对比

### 3.1 代码对比结果

**✅ 完全一致**

对比了以下部分：
- 协议定义（Header、事件常量）
- 初始化方法
- WebSocket连接处理
- 音频编码处理
- 错误处理

**结论：** 0.8.8和0.8.11的火山引擎TTS代码完全相同。

---

## 四、功能对比总结

### 4.1 豆包ASR

| 功能 | 0.8.8 | 0.8.11 | 说明 |
|------|-------|--------|------|
| **基础识别** | ✅ 支持 | ✅ 支持 | 相同 |
| **热词定制** | ✅ 支持 | ✅ 支持 | 相同 |
| **替换词** | ✅ 支持 | ✅ 支持 | 相同 |
| **多语种模式** | ❌ 不支持 | ✅ 支持 | **新功能** |
| **语言参数** | 固定zh-CN | 可配置 | **改进** |
| **WebSocket地址** | 固定bigmodel | 可切换 | **改进** |
| **停止信号处理** | 基础 | 增强 | **改进** |

### 4.2 火山引擎TTS

| 功能 | 0.8.8 | 0.8.11 | 说明 |
|------|-------|--------|------|
| **基础合成** | ✅ 支持 | ✅ 支持 | ✅ 相同 |
| **流式输出** | ✅ 支持 | ✅ 支持 | ✅ 相同 |
| **连接复用** | ✅ 支持 | ✅ 支持 | ✅ 相同 |
| **音色选择** | ✅ 支持 | ✅ 支持 | ✅ 相同 |
| **语速/音量/音高** | ✅ 支持 | ✅ 支持 | ✅ 相同 |
| **情感参数** | ✅ 支持 | ✅ 支持 | ✅ 相同 |

---

## 五、0.8.11的新功能详解

### 5.1 多语种识别模式

**配置参数：**
```yaml
asr:
  type: doubao_stream
  enable_multilingual: true  # ← 新增参数
  language: "zh-CN"          # 指定语言
```

**支持的语言：**
- 默认模式（enable_multilingual=false）：中英文、上海话、闽南语、四川话、陕西话、粤语
- 多语种模式（enable_multilingual=true）：需要指定language参数

**API差异：**
- 默认模式：`wss://openspeech.bytedance.com/api/v3/sauc/bigmodel`
- 多语种模式：`wss://openspeech.bytedance.com/api/v3/sauc/bigmodel_nostream`

### 5.2 改进的停止信号处理

**0.8.11的改进：**
```python
# 在消息结束时检查停止信号
if conn.client_listen_mode == "manual" and conn.client_voice_stop and len(audio_data) > 0:
    logger.bind(tag=TAG).debug("消息结束收到停止信号，触发处理")
    await self.handle_voice_stop(conn, audio_data)
    conn.asr_audio.clear()
    conn.reset_vad_states()
    break
```

**优点：**
- 更及时的停止响应
- 更好的manual模式支持
- 避免不必要的处理

---

## 六、数据库配置差异

### 6.1 豆包ASR配置

**0.8.8的SQL（无多语种参数）：**
```sql
-- 没有enable_multilingual和language参数
```

**0.8.11的SQL（202512301430.sql）：**
```sql
-- 更新豆包流式ASR供应器，增加配置
UPDATE `ai_model_provider` SET 
`fields` = '[
  {"key":"appid","label":"应用ID","type":"string"},
  {"key":"access_token","label":"访问令牌","type":"string"},
  {"key":"cluster","label":"集群","type":"string"},
  {"key":"boosting_table_name","label":"热词文件名称","type":"string"},
  {"key":"correct_table_name","label":"替换词文件名称","type":"string"},
  {"key":"output_dir","label":"输出目录","type":"string"},
  {"key":"end_window_size","label":"静音判定时长(ms)","type":"number"},
  {"key":"enable_multilingual","label":"是否开启多语种识别模式","type":"boolean"},
  {"key":"language","label":"指定语言编码","type":"string"}
]'
WHERE `id` = 'SYSTEM_ASR_DoubaoStreamASR';

-- 更新默认值
UPDATE `ai_model_config` SET
`config_json` = JSON_SET(
    `config_json`, 
    '$.enable_multilingual', false,
    '$.language', 'zh-CN'
)
WHERE `id` = 'ASR_DoubaoStreamASR';
```

### 6.2 火山引擎TTS配置

**0.8.11的SQL（202512301430.sql）：**
```sql
-- 更新HuoshanDoubleStreamTTS供应器配置，增加多情感音色参数
UPDATE `ai_model_provider` SET 
`fields` = '[
  {"key": "ws_url", "type": "string", "label": "WebSocket地址"},
  {"key": "appid", "type": "string", "label": "应用ID"},
  {"key": "access_token", "type": "string", "label": "访问令牌"},
  {"key": "resource_id", "type": "string", "label": "资源ID"},
  {"key": "speaker", "type": "string", "label": "默认音色"},
  {"key": "enable_ws_reuse", "type": "boolean", "label": "是否开启链接复用", "default": true},
  {"key": "speech_rate", "type": "number", "label": "语速(-50~100)"},
  {"key": "loudness_rate", "type": "number", "label": "音量(-50~100)"},
  {"key": "pitch", "type": "number", "label": "音高(-12~12)"},
  {"key": "emotion_scale", "type": "number", "label": "情感强度(1-5)"},
  {"key": "emotion", "type": "string", "label": "情感类型"}
]'
WHERE `id` = 'SYSTEM_TTS_HSDSTTS';

-- 更新默认值
UPDATE `ai_model_config` SET
`config_json` = JSON_SET(
    `config_json`,
    '$.emotion', 'neutral',
    '$.emotion_scale', 4
)
WHERE `id` = 'TTS_HuoshanDoubleStreamTTS';
```

**注意：** 虽然SQL添加了emotion参数，但代码中已经支持，所以代码层面没有差异。

---

## 七、兼容性分析

### 7.1 向后兼容性

**豆包ASR：**
- ✅ 0.8.8的配置可以在0.8.11中使用
- ✅ 默认行为保持一致（enable_multilingual=false）
- ✅ 不需要修改现有配置

**火山引擎TTS：**
- ✅ 完全兼容
- ✅ 配置可以直接迁移

### 7.2 升级建议

**从0.8.8升级到0.8.11：**

1. **豆包ASR**
   - 可以继续使用现有配置
   - 如需多语种支持，添加新参数：
     ```yaml
     enable_multilingual: true
     language: "zh-CN"
     ```

2. **火山引擎TTS**
   - 无需任何修改
   - 配置完全兼容

---

## 八、性能影响

### 8.1 豆包ASR

**多语种模式的影响：**
- 使用不同的API端点（bigmodel_nostream）
- 可能有不同的延迟特性
- 需要根据实际需求选择

**建议：**
- 如果只需要中文识别 → 使用默认模式（更快）
- 如果需要多语言识别 → 使用多语种模式

### 8.2 火山引擎TTS

**无性能差异**
- 代码完全相同
- 性能表现一致

---

## 九、总结

### 9.1 核心差异

| 组件 | 差异程度 | 主要变化 |
|------|---------|---------|
| **豆包ASR** | ⚠️ 有差异 | 新增多语种支持 |
| **火山引擎TTS** | ✅ 无差异 | 完全相同 |

### 9.2 升级影响

**豆包ASR：**
- ✅ 向后兼容
- ✅ 新增功能可选
- ✅ 默认行为不变

**火山引擎TTS：**
- ✅ 完全兼容
- ✅ 无需修改

### 9.3 推荐

**如果你使用豆包ASR：**
- 可以放心升级到0.8.11
- 现有配置继续有效
- 需要多语种时再启用新功能

**如果你使用火山引擎TTS：**
- 完全无影响
- 可以直接升级

---

**文档生成时间：** 2026-01-26  
**对比版本：** 0.8.8 vs 0.8.11  
**核心结论：** 火山引擎TTS完全相同，豆包ASR新增多语种支持但向后兼容
