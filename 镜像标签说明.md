# Docker 镜像标签说明

## 🔍 `server-acf8893` 是什么？

`server-acf8893` 是 Docker 镜像的标签（tag），其中：
- `server` - 镜像类型（服务器镜像）
- `acf8893` - Git commit SHA 的前 7 位

## 📝 标签来源

### Commit SHA

`acf8893` 是您提交代码时的 Git commit SHA（哈希值）的前 7 位。

**完整 commit SHA** 可能是：`acf8893...`（完整的是 40 位十六进制字符串）

您可以在以下位置查看完整的 commit SHA：
1. GitHub 仓库的提交历史
2. 构建日志中
3. 使用命令：`git log --oneline`

### 为什么使用 Commit SHA 作为标签？

✅ **版本追踪**：每个 commit 都有唯一的镜像版本  
✅ **可追溯性**：知道镜像对应哪个代码版本  
✅ **回滚方便**：可以轻松拉取历史版本的镜像  
✅ **避免覆盖**：不会覆盖之前的镜像版本

## 🏷️ 镜像标签策略

根据 workflow 配置，每次构建会创建**多个标签**：

### 1. 语义化标签（会更新）

- `server-base` - 最新稳定版本的基础镜像
- `server` - 最新稳定版本的服务器镜像
- `web` - 最新稳定版本的 Web 镜像

**特点**：这些标签会**指向最新的构建**，每次新构建后会自动更新。

### 2. Commit SHA 标签（永久保留）

- `server-base-acf8893` - 对应 commit `acf8893` 的基础镜像
- `server-acf8893` - 对应 commit `acf8893` 的服务器镜像
- `web-acf8893` - 对应 commit `acf8893` 的 Web 镜像

**特点**：这些标签**永久保留**，不会更新，可以随时拉取特定版本的镜像。

### 3. 分支标签

- `main` - 主分支的最新版本

### 4. Pull Request 标签

- `pr-1`, `pr-2` 等 - 对应 PR 编号的镜像

## 📦 实际示例

假设您有两次提交：

**第一次提交（commit: acf8893）**
```bash
# 创建的镜像标签：
ghcr.io/alice0422/xiaozhi-esp32-server:server-base          # 指向 acf8893
ghcr.io/alice0422/xiaozhi-esp32-server:server-base-acf8893  # 永久保留
ghcr.io/alice0422/xiaozhi-esp32-server:server               # 指向 acf8893
ghcr.io/alice0422/xiaozhi-esp32-server:server-acf8893       # 永久保留
```

**第二次提交（commit: def5678）**
```bash
# 创建的镜像标签：
ghcr.io/alice0422/xiaozhi-esp32-server:server-base          # 更新指向 def5678
ghcr.io/alice0422/xiaozhi-esp32-server:server-base-def5678  # 新版本
ghcr.io/alice0422/xiaozhi-esp32-server:server-base-acf8893  # 仍然保留！
ghcr.io/alice0422/xiaozhi-esp32-server:server               # 更新指向 def5678
ghcr.io/alice0422/xiaozhi-esp32-server:server-def5678       # 新版本
ghcr.io/alice0422/xiaozhi-esp32-server:server-acf8893       # 仍然保留！
```

## 🎯 如何使用这些标签

### 拉取最新版本（推荐用于生产）

```bash
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server
```

### 拉取特定版本（用于回滚或测试）

```bash
# 拉取 commit acf8893 的镜像
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server-acf8893
```

### 查看所有可用标签

在 GitHub Packages 页面，点击镜像名称，可以看到所有可用的标签。

## 🔧 如何修改标签策略

如果您想修改标签格式，可以编辑 `.github/workflows/docker-build.yml`：

```yaml
tags: |
  type=raw,value=server-{{sha}}        # 使用完整 SHA（40位）
  type=raw,value=server-{{sha7}}       # 使用 7 位 SHA（当前）
  type=raw,value=server-v1.0.0         # 自定义标签
```

## 📝 总结

- ✅ `acf8893` 是 Git commit SHA 的前 7 位
- ✅ 每个 commit 都会创建对应的镜像标签
- ✅ SHA 标签永久保留，不会覆盖
- ✅ `server` 标签会更新到最新版本
- ✅ 可以同时使用语义化标签和 SHA 标签

这样设计的好处是：
- 🎯 **灵活性**：可以用 `server` 拉取最新版本
- 🔒 **稳定性**：可以用 `server-acf8893` 拉取特定版本
- 📊 **可追溯**：知道每个镜像对应的代码版本

