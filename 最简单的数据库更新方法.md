# 最简单的数据库更新方法

## 方法：让manager-api自动执行SQL

manager-api服务使用Liquibase自动管理数据库迁移，只要SQL文件在正确的位置，重启服务时会自动执行。

---

## 操作步骤（3步搞定）

### 步骤1：拉取最新镜像

```bash
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:web
```

### 步骤2：重启manager-api服务

```bash
docker-compose restart xiaozhi-esp32-server-web
```

### 步骤3：检查日志确认执行

```bash
docker logs xiaozhi-esp32-server-web | grep -i "changelog"
```

**就这么简单！** manager-api会自动检测并执行这三个SQL文件：
- `202512261700.sql` - CMHK配置
- `202512301430.sql` - 阿里百炼和豆包更新
- `202601051433.sql` - FunASR更新

---

## 为什么会自动执行？

manager-api使用Liquibase管理数据库版本，配置文件在：
```
main/manager-api/src/main/resources/db/changelog/db.changelog-master.yaml
```

这个文件会自动包含所有的SQL迁移脚本，重启时自动执行未执行过的SQL。

---

## 验证是否执行成功

### 方法1：查看日志

```bash
# 查看完整日志
docker logs xiaozhi-esp32-server-web

# 搜索SQL执行记录
docker logs xiaozhi-esp32-server-web | grep "202512261700"
docker logs xiaozhi-esp32-server-web | grep "202512301430"
docker logs xiaozhi-esp32-server-web | grep "202601051433"
```

### 方法2：检查数据库

```bash
# 进入MySQL
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi

# 检查CMHK配置是否存在
SELECT * FROM ai_model_provider WHERE provider_code IN ('cmhk_xunfei_stream', 'cmhk_xtts');

# 检查Liquibase执行记录
SELECT * FROM DATABASECHANGELOG ORDER BY DATEEXECUTED DESC LIMIT 10;
```

---

## 完整更新流程（推荐）

```bash
# 1. 拉取所有最新镜像
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:server
docker pull ghcr.io/alice0422/xiaozhi-esp32-server:web

# 2. 停止所有服务
docker-compose down

# 3. 启动所有服务（manager-api会自动执行SQL）
docker-compose up -d

# 4. 查看日志确认
docker logs -f xiaozhi-esp32-server-web
```

---

## 如果自动执行失败怎么办？

### 检查db.changelog-master.yaml

查看文件内容：
```bash
cat main/manager-api/src/main/resources/db/changelog/db.changelog-master.yaml
```

应该包含：
```yaml
databaseChangeLog:
  - include:
      file: db/changelog/202512261700.sql
  - include:
      file: db/changelog/202512301430.sql
  - include:
      file: db/changelog/202601051433.sql
```

### 手动执行（备用方案）

如果自动执行失败，可以手动执行：

```bash
# 复制SQL文件到容器
docker cp main/manager-api/src/main/resources/db/changelog/202512261700.sql xiaozhi-mysql:/tmp/
docker cp main/manager-api/src/main/resources/db/changelog/202512301430.sql xiaozhi-mysql:/tmp/
docker cp main/manager-api/src/main/resources/db/changelog/202601051433.sql xiaozhi-mysql:/tmp/

# 执行SQL
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi -e "source /tmp/202512261700.sql"
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi -e "source /tmp/202512301430.sql"
docker exec -it xiaozhi-mysql mysql -uroot -p123456 xiaozhi -e "source /tmp/202601051433.sql"
```

---

## 总结

**最简单的方法就是：**
1. 拉取最新的web镜像
2. 重启manager-api服务
3. 等待自动执行完成

**不需要手动执行任何SQL！**

---

**文档生成时间：** 2026-01-26  
**适用版本：** 0.8.11  
**推荐指数：** ⭐⭐⭐⭐⭐
